import { CriaturoDndDirective } from './criaturo-dnd.directive';

describe('CriaturoDndDirective', () => {
  it('should create an instance', () => {
    const directive = new CriaturoDndDirective();
    expect(directive).toBeTruthy();
  });
});
